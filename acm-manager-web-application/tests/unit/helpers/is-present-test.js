
import { isPresent } from 'acm-manager-web-application/helpers/is-present';
import { module, test } from 'qunit';

module('Unit | Helper | is present');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isPresent([42]);
  assert.ok(result);
});

